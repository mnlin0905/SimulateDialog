<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.knowledge.mnlin.simulatedialog">

    <application
        android:name=".base.PageApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:label,android:name">

        <!--sdialog首页-->
        <activity android:name=".MainActivity" />

        <activity android:name=".SimpleFragmentActivity" />
        <activity android:name=".MultiFragmentActivity" />

        <!--程序入口-->
        <activity android:name=".MenuRouteActivity">
        </activity>

        <!--程序入口-->
        <activity android:name=".base.PageParent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>